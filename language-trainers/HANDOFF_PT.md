# Portuguese trainer — handoff

`portuguese_trainer.html` + `vocab_pt.json` — **European Portuguese** with the
Brazilian forms marked. 547 entries, 24 lessons, 12 drill types, 5 special banks.

Modelled on the Spanish trainer, and **generated from it** by
`scripts/make_portuguese_trainer.py`. Engine fixes made in Spanish flow here on
the next run — so edit the script, not the HTML.

## European primary, Brazilian marked — and both accepted

The deck teaches Portugal's Portuguese. Where Brazil says something else the
entry carries a `br` field, and three things follow:

- the reveal panel shows **Portugal: comboio · Brasil: trem** side by side;
- **both grade exact.** A learner who knows `trem` is not wrong because the
  deck's headword is `comboio`. `scripts/check_portuguese.py` runs the app's own
  `checkAnswer` over all 25 pairs and fails if any is not accepted both ways;
- the difference is visible rather than hidden, which is the point — this is a
  deck for someone who will meet both.

Three kinds of difference are marked: **vocabulary** (comboio/trem,
autocarro/ônibus, telemóvel/celular, pequeno-almoço/café da manhã), **spelling**
(the 1990 agreement removed most, but the vowel accents remain: ténis/tênis,
bebé/bebê, económico/econômico), and **grammar**, carried by phrases rather than
paradigms (chamo-me / me chamo, tenho de / tenho que, não percebo / não entendo).

## Drill types

The Spanish set, minus antonyms and synonyms (the deck links none yet), plus
three banks Spanish has no use for:

| Bank | Asks |
|---|---|
| Ser / estar / **ficar** | three-way where Spanish has two. `Lisboa {fica} em Portugal` |
| Por vs para | as Spanish |
| **Personal infinitive** | `Antes de {sairmos}, fecha a janela` — against the plain infinitive |
| **Future subjunctive** | `Se {formos} à praia` — against the personal infinitive |
| **False friends** | against Spanish: esquisito, propina, oficina, borracha |

The personal infinitive is the thing this deck can teach that the Spanish one
cannot. It is **fully regular for every verb** — ser gives *sermos*, fazer gives
*fazermos* — which is exactly why it gets confused with the future subjunctive,
which shares its endings but is built on the (wildly irregular) preterite stem:
*formos*, *fizermos*. For a regular verb the two are the same word. Both are
generated by `portuguese_morph.py` so the contrast is derived, not asserted.

Two consequences the build now enforces:

- A personal-infinitive card in the **third person singular** adds no ending, so
  it already *is* the plain infinitive and there is nothing to choose between.
  Those cards contrast the plural instead.
- A future-subjunctive card on a **regular** verb has an identical personal
  infinitive, so it contrasts the present indicative — *se falamos* for *se
  falarmos* is the mistake people actually make.

Both were found by the checks, not by reading: `check_special` fails any bank
item whose distractor equals its answer.

## A spelling oracle, and what it caught

`pyspellchecker`'s Portuguese dictionary (417k words, European-biased) runs over
every generated form. It is **advisory with an allowlist**, not absolute: it does
not know `chãos`, `limões` or `sutis`, all of which are correct, so an unknown
form must be listed in `SPELLING_OK` with a reason and anything else fails.

It caught four real rule bugs while the deck was being written, which is the
whole argument for having it:

- **-ês loses its accent in the plural**: mês→meses, português→portugueses. The
  rule was missing entirely, and -ís is the exception (país→países, where the
  accent marks a hiatus rather than the stress).
- **the -vel family takes -eis, not -éis**: possível→possíveis. A word already
  carrying an accent is stressed earlier and needs no second one; the naive rule
  produced *possívéis*, which Portuguese never writes.
- **-guer/-guir drops only the u**: erguer→ergo, not *ero*.
- **`hoje` was listed as a noun**, so the builder generated the plural *hojes*.
  A data error, caught by a spelling check.

`-ão` plurals are the lexical case, like Italian's amico/fuoco: coração→corações,
pão→pães, irmão→irmãos descend from three different Latin endings and the
spelling records none of it. `-ões` is the productive default; the other two are
curated tables in `portuguese_morph.py`.

## vós is not in the paradigm

Five persons, not six: **eu / tu / ele·ela·você / nós / eles·elas·vocês**. *Vós*
is archaic in both varieties — Portugal says *vocês* for plural you exactly as
Brazil does — so including it would be a wasted slot in every conjugation drill.

`tu` stays because this is the European deck; Brazil's default `você` takes the
third-person forms, which the deck already teaches, and the phrase entries carry
the usage difference.

## The bug worth remembering

`enabledModes` is a **second, independent list of mode names**, written as bare
object keys, so the generator's quoted-string rename did not touch it. Left
stale it enabled two modes the engine no longer had (`es_en`, `en_es`) and never
enabled the three new banks — **five of twelve drill types silently dead**, each
still shown as a pill you could switch on. Validation passed throughout, because
each mode that *could* run did.

It is now derived from `MODE_LABELS`, and `check_portuguese.py` fails if any
offered drill type generates nothing. Two more strings were missed the same way
and caught in a browser: the `<title>` and the `promptLabel: "Spanish"` eyebrow.
**Anything scaffolded from another app needs a rendered screenshot, not just a
passing validator.**

## Commands

```
python3 scripts/portuguese_morph.py            # paradigm self-check
python3 scripts/build_portuguese_vocab.py      # regenerates vocab_pt.json
python3 scripts/make_portuguese_trainer.py     # regenerates the HTML
python3 scripts/validate.py portuguese_trainer.html
python3 scripts/check_portuguese.py            # variants, banks, dead modes
```

## Known gaps

- **547 entries against Spanish's 1126.** Coverage is even across 24 lessons and
  every drill type works, but it is roughly half the Spanish deck. Nouns are the
  best served (270); verbs (88) and adjectives (61) would take the most benefit
  from another pass.
- **94 cloze sentences** across 47 headwords. The Spanish deck has them on most
  entries; here they are concentrated on the high-frequency words.
- No antonym or synonym links yet, which is why those two drill types are absent
  rather than empty.
- Only the present indicative is drilled, as in Spanish. The personal infinitive
  and future subjunctive appear only in their banks, not as full paradigm drills.
